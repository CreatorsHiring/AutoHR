<!DOCTYPE html>
<html>
<head>
  <title>Application Status | HireOrbit</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 min-h-screen flex items-center justify-center">

<div class="max-w-2xl w-full bg-white shadow-xl rounded-2xl p-8">

  <h2 class="text-2xl font-bold mb-6">Application Status</h2>

  <div class="space-y-4">

    <div>
      <p class="text-sm text-slate-500">Role Applied</p>
      <p class="font-semibold text-lg"><%= resume.jobId %></p>
    </div>

    <div>
      <p class="text-sm text-slate-500">Application Status</p>
      <p class="font-semibold 
        <%= resume.status === 'Shortlisted' ? 'text-green-600' : 
            resume.status === 'Rejected' ? 'text-red-600' : 
            'text-yellow-600' %>">
        <%= resume.status %>
      </p>
    </div>

    <div>
      <p class="text-sm text-slate-500">Assigned HR</p>
      <p class="font-semibold"><%= resume.assignedHr?.name || "Not Assigned" %></p>
    </div>

    <div>
      <p class="text-sm text-slate-500">Interview Status</p>
      <p class="font-semibold"><%= resume.interviewStatus %></p>
    </div>

    <% if (resume.selectedSlot) { %>
      <div>
        <p class="text-sm text-slate-500">Interview Scheduled For</p>
        <p class="font-semibold text-indigo-600"><%= resume.selectedSlot %></p>
      </div>
    <% } %>

  </div>

  <div class="mt-8">
    <a href="/jobs" class="px-6 py-3 bg-indigo-600 text-white rounded-lg shadow hover:bg-indigo-700 transition">
      Back to Jobs
    </a>
  </div>

</div>

</body>
</html>