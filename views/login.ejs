<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Login — HireOrbit</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="page page--auth">
    <header class="site-header">
      <nav class="navbar" aria-label="Primary">
        <div class="navbar__brand">
          <a href="/landing" class="navbar__logo" aria-label="HireOrbit home">
            <span class="navbar__logo-mark" aria-hidden="true"></span>
            <span class="navbar__logo-text">HireOrbit</span>
          </a>
        </div>

        <div class="navbar__links" id="navbar-menu">
          <a href="/landing" class="navbar__link">Home</a>
          <a href="/register" class="navbar__link">Register</a>
          <a href="/jobs" class="button button--primary navbar__cta">Explore Careers</a>
        </div>
      </nav>
    </header>

    <main class="main">
      <section class="section" aria-labelledby="login-heading">
        <div class="section__header">
          <h1 class="section__title" id="login-heading">Welcome back</h1>
          <p class="section__subtitle">Log in to continue to your job matches.</p>
        </div>

        <div class="auth">
          <div class="feature-card auth__card">
            <% if (error) { %>
              <p class="auth__error" role="alert"><%= error %></p>
            <% } %>

            <form action="/login" method="POST" autocomplete="on">
              <div class="auth__fields">
                <label class="auth__field">
                  <span class="auth__label">Email</span>
                  <input
                    name="email"
                    type="email"
                    required
                    value="<%= form?.email || '' %>"
                    autocomplete="email"
                    class="auth__input"
                  />
                </label>

                <label class="auth__field">
                  <span class="auth__label">Password</span>
                  <input
                    name="password"
                    type="password"
                    required
                    autocomplete="current-password"
                    class="auth__input"
                  />
                </label>

                <button class="button button--primary" type="submit">Login</button>

                <p class="section__subtitle auth__hint">
                  New here? <a href="/register" class="auth__link">Create an account</a>
                </p>
              </div>
            </form>
          </div>
        </div>
      </section>
    </main>

    <script src="/js/main.js" defer></script>
  </body>
</html>
